#include<tdio.h>
#include<stdlib.h>

struct Demo()
{
int i;
float f;

}:

int main()
{
strct Demo obj;

struct Demo *ptr = NULL;

ptr =( struct Demo *(sizeof(struct Demo));

printf("%d",obj.i);
printf("%d",obj->i);

return 0;
}